// This is who controls it all 
// This asks the 'models' (database) about the user
// If teh models say OK, then this tells the 'router' OK
// If not then not
const authenticate = {
	
}

module.exports = authenticate;